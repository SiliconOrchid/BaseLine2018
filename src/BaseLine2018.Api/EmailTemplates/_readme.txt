++++ Readme for EmailTemplates ++++

This folder/namespace is used to contain email templates (the .cshtml) and corresponding models.

The foldername "EmailTemplates" is intended as a project convention, but if necessary, it could be redefined.  
The default folder location is hardcoded as a static in the "RazorEngineStrategy".